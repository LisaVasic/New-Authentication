{
  "name": "project-auth-parent",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "dev": "react-scripts start" 
  },
  "dependencies": {
    "bcrypt-nodejs": "^0.0.3"
  }
}

